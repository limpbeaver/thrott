(defparameter engine
    '(0 0 0 0))

(defun cycle (engine)
    (incf (second engine)))

(defun ignition (engine)
    ;; establish heat/strain PDEs
    ;; linkage constraint sim
    ;; actuator linkage position
    )

(cycle engine)
(print engine)
